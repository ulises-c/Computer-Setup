# Ghostty Config

Located at `~/Library/Application Support/com.mitchellh.ghostty/config.ghostty` on macOS.

The raw config file is [ghostty.config](ghostty.config) — `setup.sh` copies it to the correct location automatically.

```
theme = Dark Pastel
shell-integration = zsh
```
