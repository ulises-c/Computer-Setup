title: Home Server
theme: dark
color: slate
headerStyle: clean
hideVersion: true

layout:
  Secondary Server (Pi):
    style: row
  # Management | Network | Storage side-by-side in one row (nested groups under a
  # header-less parent; homepage lays the child groups out horizontally).
  Core:
    style: row
    columns: 3
    header: false
    Management:
      style: column
    Network:
      style: column
    Storage:
      style: column
  NAS:
    style: row
    columns: 2
