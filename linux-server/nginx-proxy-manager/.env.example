# Copy this file to .env and set the value. See ../HTTPS.md for the full setup.

# Tailscale auth for the admin-UI sidecar — same OAuth client secret as the other
# sidecars (needs the Auth Keys scope + tag:container in the ACL).
TS_AUTHKEY=
