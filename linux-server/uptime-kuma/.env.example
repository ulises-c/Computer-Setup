# Copy this file to .env and update the values. See ../HTTPS.md for the full setup.

# Tailscale auth for the sidecar — the same OAuth client secret used for the
# other sidecars (it just needs the Auth Keys scope + tag:container in the ACL).
# Create at: tailscale.com/admin/settings/keys (or /oauth)
TS_AUTHKEY=
