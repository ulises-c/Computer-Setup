# Tailscale OAuth client — create at tailscale.com/admin/settings/oauth (scope: Core - Read)
# OAuth client secrets never expire, unlike API keys
TAILSCALE_CLIENT_ID=
TAILSCALE_CLIENT_SECRET=
# Device ID: tailscale status --json | jq -r '.Self.ID'
TAILSCALE_DEVICE_ID=
PORT=8089
